apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: msr
spec:
  chart:
    spec:
      chart: msr
      sourceRef:
        kind: HelmRepository
        name: msrofficial
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: msr-values
      valuesKey: values.yaml
      optional: true
  interval: 1h
  releaseName: msr
  targetNamespace: msr
